Inside the models folder there is a file called config,
 there are the connection data, depending 
 on what data you have in your mysql for example
 users and passwords there will change them
 
 at the root of the project this is a file called knowledgecityDB is the database